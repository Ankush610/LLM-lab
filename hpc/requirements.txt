# Everything both jobs need: fine-tuning (training/) and downloading weights (serving/).
# Set the env up with the commands in setup-env.txt.

# --- training ---
torch>=2.6
transformers>=4.56
trl>=0.20
peft>=0.15
bitsandbytes>=0.46
datasets>=3.6
accelerate>=1.6

# --- downloading models ---
huggingface_hub>=0.34
tqdm
